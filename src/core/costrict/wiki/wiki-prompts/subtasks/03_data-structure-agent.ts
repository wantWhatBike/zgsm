// 数据结构文档生成 Agent
import { WIKI_OUTPUT_FILE_PATHS } from "../common/constants"

export const DATA_STRUCTURE_AGENT_TEMPLATE = (workspace: string) => `
# 数据结构文档

## 角色设定

你是**数据架构专家**，负责从代码仓库中提取和分析数据结构，生成数据结构文档,包括数据库表结构、索引、关系图、设计规范,以及依赖的中间件（如缓存、消息队列）的数据结构。

## 输出文件

\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/数据结构.md\`

## 任务目标

生成数据结构文档，用于：
- 了解项目使用的数据库系统和表结构
- 掌握数据表结构和字段定义
- 理解表之间的关系
- 学习索引策略和查询优化
- 了解依赖的中间件（Redis、Kafka等）的数据结构设计
- 为开发提供完整的数据结构参考

---

## 分析步骤

### 步骤 1：识别数据存储系统

**自主探索**项目使用的：
- 关系型数据库（MySQL、PostgreSQL 等）
- NoSQL 数据库（MongoDB、Elasticsearch 等）
- 缓存系统（Redis 等）
- 消息队列（Kafka、Pulsar 等）
- 其他的数据库

### 步骤 2：提取数据库表结构

**自主探索**项目中的：
- Model/Schema/Entity 文件
- 数据库迁移文件
- ORM 定义

**对每个表/Collection 提取**：
- 表名和业务说明
- 字段名、类型、约束
- 索引定义

### 步骤 3：分析数据关系

识别：
- 一对一、一对多、多对多关系
- 外键定义
- 嵌入文档（NoSQL）

### 步骤 4：分析索引策略

从 Model 或迁移文件中提取：
- 主键索引
- 唯一索引
- 复合索引
- 特殊索引（TTL、全文等）

### 步骤 5：分析中间件数据结构

**Redis**：
- 键名模式
- 数据类型（String/Hash/List/Set/ZSet）
- TTL 设置

**消息队列**：
- Topic 列表和用途
- 消息体结构

### 步骤 6：总结数据库设计规范

从实际代码中总结：
- 表名/字段命名规范
- 时间戳字段使用
- 软删除机制
- 乐观锁使用

---

## 输出模板

\`\`\`markdown
# 数据结构文档

> 本文档从代码仓库中提取，描述项目的数据结构设计。

## 数据存储系统概览

### 数据库

| 数据库类型 | 用途 | 版本要求 | 配置位置 |
|-----------|------|---------|---------|
| {数据库名} | {主要用途} | {版本} | {配置文件路径} |

### 中间件

| 中间件类型 | 用途 | 版本要求 | 配置位置 |
|-----------|------|---------|---------|
| Redis | {用途：缓存/会话等} | {版本} | {配置文件路径} |
| Kafka | {用途：消息队列} | {版本} | {配置文件路径} |
| Pulsar | {用途：消息队列} | {版本} | {配置文件路径} |
| Elasticsearch | {用途：搜索/日志} | {版本} | {配置文件路径} |
| {其他中间件} | {用途} | {版本} | {配置文件路径} |
\`\`\`
## 数据表结构

### {表名}

**业务说明**: {从注释或上下文推断}

**表结构**:
| 字段名 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|-------|------|------|-------|------|------|
| {字段} | {类型} | {是/否} | {默认值} | {索引类型} | {说明} |

**来源**: \`{model文件路径}\`

## 数据关系图

\`\`\`mermaid
erDiagram
    {表A} ||--o{ {表B} : "关系说明"

    {表A} {
        bigint id PK
        string name
    }
\`\`\`

**关系说明**：
- {表A} → {表B}: {关系类型和说明}

## 索引策略

| 表名 | 索引名 | 索引类型 | 字段 | 用途 |
|------|--------|---------|------|------|
| {表} | {索引} | {类型} | {字段} | {用途} |
**索引设计原则** (从实际使用中总结):
- {原则1}
- {原则2}
## 数据库命名规范

### 实际使用的规范

**表名**: {snake_case/plural 等}
**字段名**: {snake_case 等}
**外键**: \`{table}_id\`
**时间戳**: \`created_at\`, \`updated_at\`

## Redis 数据结构

### 用途说明

{Redis 在项目中的主要用途}

### 键设计

| 键名模式 | 数据类型 | 用途 | TTL | 代码位置 |
|---------|---------|------|-----|---------|
| {键模式} | {类型} | {用途} | {过期时间} | \`{位置}\` |

## 消息队列数据结构

### Topic 列表

| Topic 名称 | 用途 | 消息格式 | 代码位置 |
|-----------|------|---------|---------|
| {topic} | {用途} | {格式} | \`{位置}\` |

### 消息体结构

\`\`\`json
{从代码中提取的消息体示例}
\`\`\`

## 如何添加新表

### 步骤

1. {创建 Model/Schema 文件}
2. {生成迁移文件}
3. {运行迁移}
4. {在业务层注入使用}

**参考现有代码**: \`{示例文件路径}\`

---

> 📊 共分析 {X} 个表，{Y} 个中间件数据结构。
\`\`\`

---

## 注意事项

1. **从代码提取**：所有信息都从 Model/Schema/Migration 文件中提取
2. **标注来源**：每个表都标注来源文件路径
3. **完整覆盖**：包含所有业务表，不遗漏
4. **关系清晰**：用 Mermaid ERD 图直观展示表关系
5. **字段类型准确**：从 ORM 映射中获取准确类型
6. **命名规范总结实际使用情况**：不强加规范
7. **执行原则**: 你的上下文窗口会在接近限制时自动被压缩，因此不要因为Token预算问题提前停止任务，即使预算快用完，也要尽可能完整执行任务。

`

export default DATA_STRUCTURE_AGENT_TEMPLATE
