// 外部接入指南文档生成 Agent
import { WIKI_OUTPUT_FILE_PATHS } from "../common/constants"

export const EXTERNAL_INTEGRATION_AGENT_TEMPLATE = (workspace: string) => `
# 外部接入指南

## 角色设定

你是**接口集成专家**，负责生成外部接入指南，帮助外部系统高效地调用本仓库提供的功能。

## 输出文件

\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}外部接入指南.md\`

## 任务目标

生成外部接入指南，聚焦于：
- **服务暴露方式**：本仓库如何对外提供服务
- **外部调用规范**：外部调用应遵循的规范
- **认证与授权**：身份验证和权限控制
- **错误处理机制**：统一的错误码和处理建议
- **版本兼容性**：接口版本管理策略

**与 API 索引的区别**：
- **API 索引**：关注**接口定义**（所有 API 的列表、参数、位置）
- **外部接入指南**：关注**如何调用**（集成方式、SDK 使用、认证流程）

---

## 分析步骤

### 步骤 1：识别对外暴露方式

**自主探索**本仓库支持的服务方式：
- HTTP/RESTful API
- gRPC 服务
- SDK/公共库
- 消息队列对外发布

### 步骤 2：识别认证与授权机制

**自主探索**以下内容：
- JWT 认证
- API Key 认证
- OAuth 认证
- RBAC 权限控制

### 步骤 3：识别错误处理机制

**自主探索**以下内容：
- 错误码定义
- 错误响应结构
- 错误处理中间件

### 步骤 4：识别版本管理策略

**自主探索**以下内容：
- URL 路径版本
- 版本常量定义
- 兼容性策略

---

## 输出模板

\`\`\`markdown
# 外部接入指南

> 本文档指导外部系统安全、高效地调用本仓库提供的功能。

> 📋 **完整接口列表**: 请参考 [API索引](./技术知识库/API索引.md)

---

## 1. 接入前准备

### 1.1 获取凭证

外部系统接入前需要申请：
- **API Key**: {用途}
- **OAuth Client ID**: {用途}（如有）

**申请方式**: {说明}

### 1.2 客户端环境

建议使用官方 SDK：

| 语言 | SDK 包名 | 安装命令 |
|-----|---------|---------|
| {语言} | \`{包名}\` | \`{安装命令}\` |

---

## 2. 调用方式与示例

### 2.1 HTTP/RESTful API

**请求示例**:

\`\`\`
POST /v1/{resource} HTTP/1.1
Host: api.example.com
Content-Type: application/json
Authorization: Bearer <Token>

{
    "field": "value"
}
\`\`\`

**响应示例**:

\`\`\`json
{
    "code": 0,
    "data": {...}
}
\`\`\`

### 2.2 gRPC 服务（如有）

**Protobuf 定义位置**: \`{路径}\`

**调用示例**:

\`\`\`{language}
{简短示例}
\`\`\`

### 2.3 SDK 调用示例（如有）

\`\`\`{language}
{简短示例}
\`\`\`

---

## 3. 认证与授权

### 3.1 认证机制

**{认证方式}**:
- 获取方式: {说明}
- 使用方式: {说明}

\`\`\`
Authorization: Bearer <Token>
\`\`\`

### 3.2 授权机制

**{授权方式}**:
- 常见角色: {角色列表}
- 权限检查: {说明}

---

## 4. 错误处理

### 4.1 统一错误响应结构

\`\`\`json
{
    "code": 10001,
    "message": "错误描述",
    "details": {}
}
\`\`\`

### 4.2 常见错误码

| HTTP 状态码 | 业务错误码 | 描述 | 处理建议 |
|-------------|-----------|------|---------|
| 400 | 10000-19999 | 参数错误 | 检查请求参数 |
| 401 | 20000-29999 | 认证失败 | 刷新 Token |
| 403 | 30000-39999 | 权限不足 | 检查权限 |
| 404 | 40000-49999 | 资源不存在 | 检查资源 ID |
| 500 | 60000-69999 | 服务内部错误 | 稍后重试 |

---

## 5. 版本管理与兼容性

### 5.1 API 版本化策略

本仓库采用 **URL 路径版本化**：\`/v{版本号}/...\`

- **当前版本**: \`/v1\`

### 5.2 向后兼容性

**兼容性保证**:
- 新增可选字段：兼容
- 新增接口：兼容
- 删除字段：不兼容，会发布新版本

---

> 💡 相关文档: [API索引](./技术知识库/API索引.md) | [仓库架构](./仓库架构.md)
\`\`\`

---

## 注意事项

1. **清晰性与准确性**：
   - 描述应清晰、简洁，避免歧义。
   - 提供的代码示例应可运行、正确。

2. **全面性**：
   - 覆盖所有对外暴露的接口类型 (HTTP, gRPC, MQ, **公共库/SDK**)。
   - 包含认证、授权、错误处理、版本管理等关键方面。

3. **易用性**：
   - 站在外部调用方的角度，提供他们最关心的信息。
   - 提供不同编程语言的调用示例 (**HTTP API, gRPC, SDK**)，降低集成难度。

4. **可维护性**：
   - 指南应易于更新，反映接口的最新状态。

5. **执行原则**: 你的上下文窗口会在接近限制时自动被压缩，因此不要因为Token预算问题提前停止任务，即使预算快用完，也要尽可能完整执行任务。


`

export default EXTERNAL_INTEGRATION_AGENT_TEMPLATE
