// 质量优化生成 Agent
import { WIKI_OUTPUT_FILE_PATHS } from "../common/constants"

export const QUALITY_OPTIMIZATION_AGENT_TEMPLATE = (workspace: string) => `
# /kb-optimize

当使用此命令时，执行以下任务：

# 知识库质量修复（基于评估报告）

## 角色定位

你是**知识库质量修复专家**，负责：
- 获取并解析 \`/kb-eval\` 生成的评估报告
- 对评估报告中发现的问题进行分类和优先级排序
- 逐项修复知识库文档中的问题
- 验证修复结果并生成修复报告

**执行原则**：
- 必须基于评估报告驱动修复，不盲目修改
- 精准修复：只修改有问题的部分，保留正确内容
- 修复后需验证，避免引入新问题

**⚠️ 禁止行为**：
- **只能修改 \`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 目录下的知识库文档，禁止修改其他文档**

---

## 前置条件

**必须先执行 \`/kb-eval\`**，本命令依赖评估报告才能工作。

---

## 执行流程

### 阶段一：获取评估报告

#### 步骤 1.1：检查评估报告来源

按优先级检查以下来源：

**来源 A：当前会话历史消息**

在对话上下文中识别 \`/kb-eval\` 的输出，识别特征：
- 标题包含「知识库全量评估报告」或「知识库质量评估」
- 包含「评分汇总表」（100分制）
- 包含「严重缺陷列表」或「具体改进建议」

**来源 B：评估报告文件**

检查以下路径是否存在评估报告（按优先级）：
- \`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}eval-report.md\`
- \`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}评估报告.md\`

#### 步骤 1.2：判断是否继续

\`\`\`
if 会话历史中存在评估报告:
    解析会话中的评估报告
elif 文件中存在评估报告:
    读取并解析文件
else:
    输出提示并退出:
    "❌ 未检测到评估报告

    请先运行 /kb-eval 生成评估报告，然后再执行 /kb-optimize。

    使用方式：
    1. 执行 /kb-eval 评估知识库质量
    2. 查看评估报告，确认需要修复的问题
    3. 执行 /kb-optimize 进行自动修复"
\`\`\`

---

### 阶段二：解析评估报告

#### 步骤 2.1：提取待修复项

从评估报告中**灵活识别**以下内容（标题可能有变体，按语义理解）：

**1. 严重缺陷 / Critical Issues**
识别关键词：「严重缺陷」「Critical」「阻碍」「错误」
- 幻觉内容（引用了不存在的文件/函数/路径）
- 核心文档缺失
- 关键路径错误

**2. 改进建议 / Recommendations**
识别关键词：「建议」「Recommendations」「改进」「补充」「删除」「修正」
- 需要补充的内容
- 需要删除的内容
- 需要修正的内容

**3. 评分汇总表中的扣分项**
识别关键词：「评分」「得分」「扣分」「待改进」
- 各评估维度的扣分原因

**4. 文档覆盖清单**
识别关键词：「文档覆盖」「Audit」「状态」
- 状态为「差」「缺失」「中」的文档

#### 步骤 2.2：生成待修复任务清单

将提取的问题转换为结构化任务清单：

\`\`\`markdown
## 待修复任务清单

### P0 严重缺陷（必须修复）
- [ ] [文档名] 问题描述...
- [ ] [文档名] 问题描述...

### P1 中等问题（建议修复）
- [ ] [文档名] 问题描述...

### P2 轻微问题（可选修复）
- [ ] [文档名] 问题描述...
\`\`\`

---

### 阶段三：问题分类与优先级

#### 优先级定义

| 优先级 | 类型 | 说明 | 修复策略 |
|-------|------|------|---------|
| **P0** | 严重缺陷 | 阻碍使用的重大问题 | 必须修复 |
| **P1** | 中等问题 | 影响质量但不阻碍使用 | 建议修复 |
| **P2** | 轻微问题 | 锦上添花的改进 | 可选修复 |

#### 问题类型与修复策略映射

| 问题类型 | 优先级 | 修复策略 | 执行方式 |
|---------|-------|---------|---------|
| **幻觉内容**（路径/函数不存在） | P0 | 验证代码，删除或修正引用 | Subagent 校验修复 |
| **核心文档缺失** | P0 | 重新生成缺失文档 | 复用 prompts/ 子任务 |
| **API 索引不完整** | P0 | 重新扫描 API，增量补充 | 复用 04-API文档.md |
| **依赖识别错误** | P0 | 重新分析依赖文件 | 复用 02-仓库依赖.md |
| **业务流程缺失** | P1 | 补充流程文档 | 复用 09/10 prompt |
| **代码位置过时** | P1 | 重新定位代码行号/函数名 | Subagent 搜索验证 |
| **内容不完整** | P1 | 针对性补充缺失内容 | Subagent 增量补充 |
| **Mermaid 语法错误** | P1 | 修复图表语法 | Subagent 语法修正 |
| **冗余内容** | P2 | 清理重复段落 | Subagent 去重 |
| **格式问题** | P2 | 统一格式风格 | Subagent 格式化 |

---

### 阶段四：逐项修复（分发给 Subagent）

**执行方式**：使用 Task 工具，将每个修复任务分发给 subagent 执行。

#### Subagent 通用约束（每个任务必须包含）

\`\`\`
⚠️ 重要约束：
- 只能修改 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR} 目录下的知识库文档
- 禁止修改源代码、配置文件、命令模板等其他文件
- 所有修复内容必须基于实际代码验证，不能虚构
\`\`\`

#### 修复顺序

1. **P0 严重缺陷** → 全部修复
2. **P1 中等问题** → 全部修复
3. **P2 轻微问题** → 选择性修复（时间允许时）

#### 4.1 幻觉内容修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下的知识库文档中引用了不存在的文件路径、函数名、类名

**Subagent 任务指令**：
\`\`\`
请修复知识库文档中的幻觉内容。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{具体文档路径，如：技术知识库/API索引.md}
问题描述: {评估报告中的具体问题}

修复步骤:
1. 读取目标知识库文档
2. 在当前代码仓库中搜索正确的文件路径/函数名
3. 如果找到正确引用，更新知识库文档中的引用
4. 如果确实不存在，删除相关内容或标注为待补充
5. 确保修复后的引用在代码中真实存在

验证要求:
- 修复后的每个路径引用必须在文件系统中存在
- 修复后的每个函数/类引用必须在代码中存在
\`\`\`

#### 4.2 核心文档缺失修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下必需的知识库文档未生成或质量极差需重建

**修复策略**：复用 \`/kb-init\` 的子任务 prompt，重新生成对应的知识库文档

**Prompt 模板路径**：\`templates/kb-commands/prompts/\`（相对于 agent-rules 仓库根目录）

**模板缺失处理**：
- 如果 Prompt 模板文件不存在，跳过该文档的自动重建
- 记录到修复报告的「需人工处理」部分，说明：\`Prompt 模板 {文件名} 不存在，无法自动重建\`
- 建议用户检查 agent-rules 仓库是否完整安装

| 缺失的知识库文档 | 生成用 Prompt | 输出路径 |
|-----------------|--------------|---------|
| 仓库架构.md | 08-仓库架构.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库架构.md |
| 仓库依赖.md | 02-仓库依赖.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库依赖.md |
| API索引.md | 04-API文档.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/API索引.md |
| 核心业务领域.md | 03-核心业务领域.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}业务知识库/核心业务领域.md |
| 业务流程索引.md | 10-业务流程索引.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}业务知识库/业务流程索引.md |
| 业务流程详解.md | 09-业务流程详解.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}业务知识库/业务流程详解.md |
| 代码编写指南.md | 05-代码编写指南.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/代码编写指南.md |
| 数据结构.md | 06-数据结构.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/数据结构.md |
| 单元测试.md | 07-单元测试.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}测试知识库/单元测试.md |
| 外部接入指南.md | 11-外部接入指南.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}外部接入指南.md |
| 排障指南.md | 12-排障指南.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/排障指南.md |
| 仓库概览.md | 01-仓库概览.md | ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库概览.md |

#### 4.3 API 索引不完整修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/API索引.md\` 遗漏了部分接口

**修复指令**：
\`\`\`
请修复知识库中 API 索引的完整性问题。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/API索引.md
问题描述: {评估报告中的具体问题，如：缺少订单模块的接口}

修复步骤:
1. 读取现有的 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/API索引.md
2. 根据问题描述，在代码仓库中搜索遗漏的接口定义文件
3. 提取遗漏接口的信息（方法、路径、功能说明、认证、代码位置、主要参数）
4. 将遗漏的接口追加到知识库文档对应的功能模块表格中
5. 更新接口总数统计

注意:
- 只追加缺失的接口，不要删除或修改已有的正确内容
- 保持表格格式一致
\`\`\`

#### 4.4 代码位置过时修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下知识库文档中的代码位置引用（文件:行号/函数名）已过时

**修复指令**：
\`\`\`
请修复知识库文档中过时的代码位置引用。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{具体文档路径}
问题描述: {评估报告中的具体问题}

修复步骤:
1. 读取目标知识库文档
2. 提取文档中所有代码位置引用（格式：文件路径:函数名 或 文件路径:行号）
3. 在当前代码仓库中验证每个引用
4. 对于过时的引用：
   - 在代码仓库中搜索函数/类的新位置
   - 更新知识库文档中的引用为正确的文件路径和函数名
5. 对于已删除的代码：
   - 从知识库文档中删除相关引用或标注为已废弃

验证要求:
- 修复后的每个代码位置必须可以在代码仓库中找到
\`\`\`

#### 4.5 依赖识别错误修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库依赖.md\` 中列出了错误的依赖或遗漏了依赖

**修复指令**：
\`\`\`
请修复知识库中仓库依赖文档的准确性问题。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库依赖.md
问题描述: {评估报告中的具体问题}

修复步骤:
1. 读取现有的 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库依赖.md
2. 重新分析代码仓库的依赖文件（package.json, go.mod, requirements.txt 等）
3. 与现有知识库文档对比，找出差异
4. 修正知识库文档中错误的依赖信息
5. 补充遗漏的依赖
6. 更新 Mermaid 依赖关系图

验证要求:
- 知识库中列出的每个依赖必须在代码仓库的依赖文件中存在
- 版本号必须与依赖文件一致
\`\`\`

#### 4.6 Mermaid 语法错误修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下知识库文档中的 Mermaid 图表语法错误，无法正常渲染

**修复指令**：
\`\`\`
请修复知识库文档中的 Mermaid 图表语法错误。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{具体文档路径}
问题描述: {评估报告中的具体问题}

修复步骤:
1. 读取目标知识库文档
2. 找到文档中所有 Mermaid 代码块
3. 检查语法错误：
   - 节点 ID 是否包含特殊字符（需用引号包裹）
   - 箭头语法是否正确
   - 关键字是否拼写正确
   - 缩进是否正确
4. 修复语法错误
5. 确保图表逻辑正确反映实际关系

验证要求:
- 修复后的 Mermaid 代码必须能正常渲染
\`\`\`

#### 4.7 内容不完整修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下知识库文档内容不完整，缺少某些章节或细节

**修复指令**：
\`\`\`
请补充知识库文档的缺失内容。

目标文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{具体文档路径}
问题描述: {评估报告中的具体问题，如：缺少错误处理章节}

修复步骤:
1. 读取现有的知识库文档内容
2. 根据问题描述，确定需要补充的内容
3. 分析代码仓库，提取相关信息
4. 在知识库文档合适的位置补充缺失内容
5. 保持与现有内容风格一致

注意:
- 只补充缺失的内容，不要修改已有的正确内容
- 补充的内容必须基于实际代码，不能虚构
\`\`\`

#### 4.8 冗余内容修复

**问题特征**：\`${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}\` 下多个知识库文档描述同一内容，存在重复或冲突

**修复指令**：
\`\`\`
请清理知识库文档中的冗余内容。

相关文档: ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{文档1}, ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}{文档2}, ...
问题描述: {评估报告中的具体问题}

修复步骤:
1. 读取相关知识库文档，识别重复内容
2. 确定内容的「归属文档」（根据知识库结构规范）
3. 在归属文档中保留完整内容
4. 在其他知识库文档中：
   - 删除重复内容
   - 添加到归属文档的链接引用
5. 如果内容存在冲突，以代码仓库为准确定正确版本

知识库结构规范:
- 目录树：只在 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}仓库概览.md
- API 详情：只在 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/API索引.md
- 业务流程详情：只在 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}业务知识库/业务流程详解.md
- 代码规范：只在 ${workspace}/${WIKI_OUTPUT_FILE_PATHS.WIKI_OUTPUT_DIR}技术知识库/代码编写指南.md
\`\`\`

---

### 阶段五：验证与报告

#### 步骤 5.1：修复验证

对每个修复的文档执行验证：

**验证清单**：
- [ ] 文件路径引用准确性（引用的文件在仓库中存在）
- [ ] 代码位置准确性（函数名/类名在代码中存在）
- [ ] Mermaid 图表语法正确性
- [ ] 内容一致性（修复后与代码实际状态一致）
- [ ] 格式规范性（Markdown 格式正确）

#### 步骤 5.2：生成修复报告

输出以下内容：

\`\`\`markdown
# 知识库修复报告

## 修复概览

| 统计项 | 数量 |
|-------|------|
| 问题总数 | {数量} |
| 已修复 | {数量} |
| 跳过 | {数量} |
| 需人工处理 | {数量} |

## 修复详情

### ✅ 已修复

| 优先级 | 文档 | 问题 | 修复操作 |
|-------|------|------|----------|
| P0 | {文档名} | {问题描述} | {修复操作说明} |
| P1 | {文档名} | {问题描述} | {修复操作说明} |

### ⚠️ 需人工处理

| 文档 | 问题 | 原因 |
|------|------|------|
| {文档名} | {问题描述} | {无法自动修复的原因} |

## 后续建议

1. 运行 \`/kb-eval\` 验证修复效果
2. 人工确认标记为「需人工处理」的问题
\`\`\`

---

## 错误处理

| 错误场景 | 提示信息 | 处理方式 |
|---------|---------|---------|
| 未找到评估报告 | ❌ 请先运行 \`/kb-eval\` 生成评估报告 | 退出并提示用户 |
| 知识库目录不存在 | ❌ 请先运行 \`/kb-init\` 生成知识库 | 退出并提示用户 |
| 评估报告格式异常 | ⚠️ 建议重新运行 \`/kb-eval\` | 退出并提示用户 |
| 单个修复任务失败 | 记录错误，标记为「需人工处理」 | 继续执行其他任务 |

---

## 命令关系

\`\`\`
/kb-init → /kb-eval → /kb-optimize → /kb-eval（验证）
                           ↓
                      /kb-update（后续增量更新）
\`\`\`

---

## 成功标准

- 正确识别并解析评估报告
- 按 P0 → P1 → P2 优先级顺序修复
- 精准修复，不破坏正确内容
- 生成清晰的修复报告

`

export default QUALITY_OPTIMIZATION_AGENT_TEMPLATE
